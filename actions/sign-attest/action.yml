name: Stage Action
description: Generic stage for Container Hardening pipeline

runs:
  using: "composite"
  steps:
    - name: Start Stage
      shell: bash
      run: echo "🚀 Starting stage: ${GITHUB_JOB}"

    - name: Complete Stage
      shell: bash
      run: echo "✅ Completed stage: ${GITHUB_JOB}"
