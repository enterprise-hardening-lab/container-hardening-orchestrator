name: Stage Action
description: Generic stage for Container Hardening pipeline

runs:
  using: "composite"
  steps:
    - name: Start stage
      shell: bash
      run: |
        echo "🚀 Starting stage: ${GITHUB_JOB}"
        echo "Repository: ${GITHUB_REPOSITORY}"
        echo "Workflow: ${GITHUB_WORKFLOW}"

    - name: Complete stage
      shell: bash
      run: |
        echo "✅ Completed stage: ${GITHUB_JOB}"
